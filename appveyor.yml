# Test against this version of Node.js
environment:
  nodejs_version: "2.5"

platform:
  - x86

# Install scripts. (runs after repo cloning)
install:
  # Get the latest stable version of Node.js or io.js
  - ps: Install-Product node $env:nodejs_version
  # install modules
  - npm install

# Post-install test scripts.
test_script:
  # - npm test
  - node ./distribute.js

artifacts:
  - path: *.zip

# Don't actually build.
build: off

deploy:
  provider: FTP
  protocol: sftp
  host: emmet.io
  username:
    secure: RNct7AzJiLB2Si0NeO89GA==
  password:
    secure: PntDHuKJGl6KzbrP5h1avVWzI2EWFDizQ2hi1MZfWLk=
  folder: livestyle
